# Expo/Metro can't resolve nested deps (e.g. expo-modules-core) under pnpm's
# default isolated node_modules. Hoist to a flat layout, as Expo recommends
# for monorepos: https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
